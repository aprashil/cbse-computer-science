# Module: Statistics

The statistics module allows us to perform advanced statistical calculations in python.
> import statistics

## Methods:
1) `mean(array)`: finds the mean of all the elements in the array.
2) `median(array)`: finds the median of all the elements in the array.
3) `mode(array)`: finds the mode of all the elements in the array.
